# Data files (CSV exports, BQ pulls, etc.) — never commit
*.csv

# Credentials — never commit any JSON (service account keys are JSON)
*.json
!package.json
!package-lock.json
.streamlit/secrets.toml
.env
.env.*
!.env.example

# Local saved state
saved_views.json

# Python venv & caches
.venv/
__pycache__/
*.pyc
.pytest_cache/

# OS / IDE
.DS_Store
.idea/
.vscode/
